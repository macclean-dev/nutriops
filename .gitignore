dist/
.DS_Store
src/.DS_Store
HANDOFF.md

# Local environment & secrets — nunca commitar
.env
.env.local
.env.*.local

# Local Claude Code config (não fazer parte do repo)
.claude/

# Dependências (reproduzíveis via package-lock.json + npm ci)
node_modules/

# OS / editor
*.swp
.idea/
.vscode/
