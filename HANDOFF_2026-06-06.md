# NutriOPS — Handoff de sessão (06/06/2026)

> Documento local (NÃO commitar). Leia `CLAUDE.md` e `project_specs.md` primeiro.
> Estado atual e o que importa pra próxima sessão.

---

## TL;DR

**Versão em produção:** `v1.9.5` em `nutriops.uniwares.net` (auto-deploy no push).

Sessão de UI + operação. Três blocos:
1. **UI entregue** — seletor de empresa no header, breadcrumb nos hubs,
   polimento do login + ⌘K. Tudo validado no browser e em produção.
2. **Auth+RLS decidido** — decisões #1 (híbrido) e #2 (device-token) aprovadas;
   #3 sem staging (testa em prod). Épico **pausado** até fechar as lojas.
3. **Sync das lojas** — Swiss conectada e sincronizando; Bäckerei no ar mas
   parada em 04/06; DBK ainda zerada.

**Saúde:** 136 testes verdes · bundle 97 KB gzip · build verde · prod no ar.

---

## Commits desta sessão (todos no main)

```
ed1125f docs: decisão #3 (sem staging) + épico pausado p/ Swiss (v1.9.4)
a4d99cb docs: aprova decisões #1 e #2 do épico Auth+RLS (v1.9.3)
da66132 docs: convenção de versionamento por commit (v1.9.2)
2762996 chore: bump APP_VERSION 1.9.0 → 1.9.1
0f197db feat: polimento do login + ⌘K alinhado à troca de empresa
f6090bb feat: breadcrumb nos hubs + esconde barra de tabs com 1 sub-view
d75f412 feat: seletor de empresa no header com relogin por PIN
```
(+ o commit de docs/version desta atualização → v1.9.5)

Arquivos novos: `src/user-match.js`, `src/user-match.test.js`.

---

## Estado das lojas (SQL de 06/06)

| Loja | Nuvem (temperatura) | Último registro | Status |
|------|---------------------|-----------------|--------|
| **swiss** | 77 | hoje 13:10 | ✅ sincronizando ponta a ponta (fila 92→0, testWrite ok) |
| **backerei** | 18 | 04/06 22:15 | ⚠️ no ar mas parada — verificar device |
| **dbk-producao** | 0 | — | 🔴 ainda não conectada |

> O "~95" estimado antes era chute alto — o "91" do banner contava TODOS os
> módulos, não só temperatura. 77 só de temperatura é coerente pra Swiss.

### Pendente nas lojas (sem urgência — devices se auto-curam no boot)
1. **Swiss:** check local×nuvem pra aposentar dúvida de registros pré-fila:
   `JSON.parse(localStorage.getItem('nutriops.temperature.records')||'[]').filter(r=>r.tenantId==='swiss').length`
   Se > 77 → Migrar via admin (login de admin global → Configurações → Migração).
2. **Bäckerei:** mesmo check com `'backerei'`; investigar parada em 04/06.
3. **DBK:** habilitar Supabase no device (auto-config no boot deve ligar) +
   Migrar via admin. Receita completa no CLAUDE.md.

Roteiro de campo impresso: `~/Documents/NutriOPS-roteiro-migracao-estacoes.pdf`.

---

## Auth + RLS — decisões travadas (épico pausado)

- ✅ #1 modelo híbrido · ✅ #2 device-token por tenant · ⚠️ #3 sem staging
  (testa em prod com RLS off na Fase 0; ligar RLS tenant-por-tenant depois).
- Plano completo em `docs/AUTH_RLS_PLAN.md`. Retomar **depois** de fechar as 3
  lojas no sync.

---

## Supabase usage — investigado e descartado

Aviso de "exceeding usage limits" **não é o NutriOPS**: banco inteiro < 2 MB,
auth vazio, dead tuples irrisórios (vacuum inútil). As tabelas que aparecem são
internas de Auth/Storage de outro projeto (provável Nexum) na mesma org — o
egress free tier (5 GB/mês) é compartilhado pela organização. Conferir em
Dashboard → Organization → Usage o detalhamento por projeto.

---

## Próxima sessão — ordem sugerida
1. **Fechar as lojas** quando houver acesso aos devices: DBK (zerada) é a
   prioridade, depois Bäckerei e o check da Swiss.
2. **Retomar o épico Auth+RLS** (Fase 0 — escrever policies em prod com RLS off).
3. Backlog menor: equipamento duplicado no catálogo da Swiss (ver handoff 30/05).
